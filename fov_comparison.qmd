# Field of View Comparison

In imaging datasets like FLIM data, field of view is an important categorical feature that indicates where a certain set of rows (e.g. cells) originates. Visualizing each image side by side on a numerical feature can help 