\usepackage{fontspec}
\directlua{luaotfload.add_fallback("emojifallback", {"Apple Color Emoji:mode=harf;"})}
\setmainfont{Times New Roman}[RawFeature={fallback=emojifallback}]

\makeatletter
\AtBeginDocument{%
  \hypersetup{%
    pdftitle={Supplemental Manual},
    pdfauthor={}
  }%
  \renewcommand{\maketitle}{%
    \begin{titlepage}
      \thispagestyle{empty}
      \centering
      {\Huge\bfseries\itshape Supplemental Manual\par}
      \vfill
    \end{titlepage}
  }%
}
\makeatother
