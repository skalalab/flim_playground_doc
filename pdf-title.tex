\usepackage{fontspec}
\directlua{luaotfload.add_fallback("emojifallback", {"Apple Color Emoji:mode=harf;"})}
\setmainfont{Times New Roman}[RawFeature={fallback=emojifallback}]

\makeatletter
\AtBeginDocument{%
  \hypersetup{%
    pdftitle={Methods S1: FLIM Playground Manual},
    pdfauthor={}
  }%
  \renewcommand{\maketitle}{%
    \begin{titlepage}
      \thispagestyle{empty}
      \centering
      {\Huge\bfseries\itshape Methods S1: FLIM Playground Manual\par}
      \vspace{1em}
      {\Large\itshape (related to STAR Methods)\par}
      \vfill
    \end{titlepage}
  }%
}
\makeatother
